---
layout: layout.njk
title: Gallery
---

<div class="gallery">
  {% for image in gallery %}
    <img src="{{ image.src }}" alt="{{ image.alt }}">
    {# <figure> #}
      {# <img src="{{ image.src }}" alt="{{ image.alt }}"> #}
      {# <figcaption>{{ image.caption }}</figcaption> #}
      {# </figure> #}
    {% endfor %}
  </div>