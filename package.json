{
  "name": "11ty",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "start": "eleventy --serve",
    "bundle": "eleventy",
    "docker:build": "docker build -t catallenya-blog .",
    "docker:run": "docker run -p 8080:80 catallenya-blog",
    "docker:preview": "bun docker:build && bun docker:run"
  }
}
